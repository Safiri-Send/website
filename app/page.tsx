import React from "react";
import DeviceCompatibility from "@/app/components/DeviceCompatibility";

const page = () => {
  return (
    <div>
      <DeviceCompatibility />
    </div>
  );
};

export default page;
